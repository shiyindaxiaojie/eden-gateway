spring:
  cloud:
    nacos:
      config:
        username: nacos
        password:
        server-addr: localhost:8848
        namespace: demo
        group: DEFAULT_GROUP
      discovery:
        username: nacos
        password:
        server-addr: localhost:8848
        namespace: demo
        group: DEFAULT_GROUP
